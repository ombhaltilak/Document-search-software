# ODF - Offline Document Finder
# Minimal runtime dependencies (Python 3.8+)

# Vector database (local, no server needed)
chromadb>=0.4.22

# AI embedding engine (ONNX-based, no PyTorch needed)
fastembed>=0.2.0

# Document text extraction
pdfminer.six>=20220524
python-docx>=0.8.11

# UI
customtkinter
pillow

# Global hotkey (Ctrl+K)
pynput

# Core utilities
numpy>=1.21.0,<2.0.0
tqdm>=4.60.0
requests>=2.25.0
